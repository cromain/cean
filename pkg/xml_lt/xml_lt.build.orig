
pkg_make()
{
    [ $# -ge 2 ] || exit
    srcdir=$1; tmpdir=$2
    
    cd $srcdir
    mkdir -p src include ebin doc priv
    make
    [ $? -eq 0 ] || exit 3
    cp *\.erl src
    cp *\.beam ebin
    cp *\.html doc
    cp *\.xml *\.dtd priv
}

