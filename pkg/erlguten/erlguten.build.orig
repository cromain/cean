
pkg_make()
{
    [ $# -ge 2 ] || exit
    srcdir=$1; tmpdir=$2
    
    cd $srcdir
    make
    [ $? -eq 0 ] || exit 3
    mkdir -p src include ebin
    cp *erl src
    cp *hrl include
    cp *beam ebin
}

