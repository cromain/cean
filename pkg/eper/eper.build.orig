pkg_make()
{
    [ $# -ge 2 ] || exit
    srcdir=$1; tmpdir=$2
    
    cd $srcdir
    ceansh erl.compile devel -o ebin src/*erl
    [ $? -eq 0 ] || exit 3
    mkdir -p priv/glade priv/scripts
    cp src/*\.glade* priv/glade
    cp -r bin/* priv/scripts
    rm priv/scripts/Makefile*
}

