#!/bin/sh

[ $# -ge 1 ] || {
    echo "usage: cean_call workdir [function args]"
    exit 1
}

export ZDOTDIR=$HOME/.cean
work=$1; shift
call=$1; shift
args=$*

cd $work
[ "$call" = ""] && zsh || zsh -i ~/.cean/functions/$call $args
